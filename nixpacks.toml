[phases.setup]
nixPkgs = ['python311', 'libGL']

[phases.install]
cmds = [
  'pip install --upgrade pip setuptools wheel',
  'pip install -r requirements.txt'
]

[start]
cmd = 'gunicorn fixed_ultra_server:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2'
